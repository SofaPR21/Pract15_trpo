<Window x:Class="Практическая__15.Views.SimpleInputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Ввод данных" 
        Height="210" 
        Width="410"
        WindowStartupLocation="CenterOwner"
        Background="#dfeaeb"
        ResizeMode="NoResize">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" 
                   x:Name="txtPrompt" 
                   Text="Введите название:" 
                   FontSize="14"
                   Margin="0,0,0,15"
                   Foreground="#333333"/>

        <TextBox Grid.Row="1" 
                 x:Name="txtInput" 
                 Height="35"
                 FontSize="14"
                 Padding="5"
                 Margin="0,0,0,20"
                 Background="White"
                 BorderBrush="#E0E0E0"
                 BorderThickness="1"/>

        <StackPanel Grid.Row="2" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right">
            <Button Content="OK" 
                    Click="Ok_Click" 
                    Width="80" 
                    Height="35"
                    Margin="5"
                    Background="#00c4b4" 
                    Foreground="White"
                    FontWeight="Bold"
                    Cursor="Hand"/>
            <Button Content="Отмена" 
                    Click="Cancel_Click" 
                    Width="100" 
                    Height="35"
                    Margin="5"
                    Background="#E0E0E0"
                    Foreground="#333333"
                    FontWeight="Bold"
                    Cursor="Hand"/>
        </StackPanel>
    </Grid>
</Window>